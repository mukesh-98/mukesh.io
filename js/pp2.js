
var animation = bodymovin.loadAnimation({
container: document.getElementById('about-image'), // Required
path: './js/14583-multi-tasking.json', // Required
renderer: 'svg', // Required
loop: true, // Optional
autoplay: true, // Optional

})


var animation = bodymovin.loadAnimation({
    container: document.getElementById('bm'), // Required
    path: './js/14444-sunny.json', // Required
    renderer: 'svg', // Required
    loop: true, // Optional
    autoplay: true, // Optional

    })


var animation = bodymovin.loadAnimation({
    container: document.getElementById('bm1'), // Required
    path: './js/14027-software-engineer.json', // Required
    renderer: 'svg', // Required
    loop: true, // Optional
    autoplay: true, // Optional

    })
