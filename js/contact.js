var animation = bodymovin.loadAnimation({
    container: document.getElementById('img1'), // Required
    path: 'js/6210-social-media-marketing.json', // Required
    renderer: 'svg', // Required
    loop: true, // Optional
    autoplay: true, // Optional
    
    })

    var animation = bodymovin.loadAnimation({
        container: document.getElementById('img2'), // Required
        path: 'js/13092-contact-us.json', // Required
        renderer: 'svg', // Required
        loop: true, // Optional
        autoplay: true, // Optional
        
        })

        var animation = bodymovin.loadAnimation({
            container: document.getElementById('img3'), // Required
            path: 'js/3046-me-at-office.json', // Required
            renderer: 'svg', // Required
            loop: true, // Optional
            autoplay: true, // Optional
            
            })