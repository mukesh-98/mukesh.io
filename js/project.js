var animation = bodymovin.loadAnimation({
    container: document.getElementById('about-img1'), // Required
    path: 'js/9867-github-logo-octocat-animated.json', // Required
    renderer: 'svg', // Required
    loop: true, // Optional
    autoplay: true, // Optional
    
    })